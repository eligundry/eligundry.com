---
interface Props {
  src: string
  alt: string
  className?: string
}

const { src, alt, className } = Astro.props
---

<video autoplay loop muted class:list={['mx-auto', className]}>
  <source src={src} />
  {alt}
</video>
