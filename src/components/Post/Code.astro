---

---

{
  Astro.props.class && (
    <pre class:list={['astro-code', 'astro-code-compact', Astro.props.class]}>
      <slot />
    </pre>
  )
}
{
  !Astro.props.class && (
    <pre>
      <slot />
    </pre>
  )
}
